<!--pages/user/addressList/addressList.wxml-->
<view class="address-list">
    <view class="notice" wx:if="{{!list.length}}">暂无收货地址</view>
    <button bindtap="add" class="address-add">新建地址</button>
</view>
